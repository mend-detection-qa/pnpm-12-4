[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ml-service"
version = "0.1.0"
description = "Python sub-project: ML inference service"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "certifi>=2024.2.2",
]
